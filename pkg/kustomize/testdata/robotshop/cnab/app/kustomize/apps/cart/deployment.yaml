apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: deployment
spec:
  template:
    spec:
      containers:
      - name: dummy-image
        ports:
        - containerPort: 8080
          protocol: TCP
